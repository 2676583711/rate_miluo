/*package com.rate.system.rate_system.service.impl;

import java.util.List;
import java.util.Map;
import java.util.UUID;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.rate.system.rate_system.dao.PersonDao;
import com.rate.system.rate_system.entity.Person;
import com.rate.system.rate_system.service.PersonService;



@Service
public class PersonServiceImpl implements PersonService {
	@Autowired
	private PersonDao personDao;
	
	@Override
	public Person get(String id){
		return personDao.get(id);
	}
	
	@Override
	public List<Person> list(Map<String, Object> map){
		return personDao.list(map);
	}
	
	@Override
	public int count(Map<String, Object> map){
		return personDao.count(map);
	}
	
	@Override
	public int save(Person person){
		String strId = UUID.randomUUID().toString().replaceAll("-", "");
		person.setId(strId);
		return personDao.save(person);
	}
	
	@Override
	public int update(Person person){
		return personDao.update(person);
	}
	
	@Override
	public int remove(String id){
		return personDao.remove(id);
	}
	
	@Override
	public int batchRemove(String[] ids){
		return personDao.batchRemove(ids);
	}
	
}
*/